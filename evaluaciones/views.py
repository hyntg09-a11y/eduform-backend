"""
evaluaciones/views.py

Este archivo contenía vistas de API (usando Django REST Framework) que parecen
ser una implementación duplicada de la lógica que ya existe en `frontend/views.py`.

- `frontend/views.py` maneja la lógica para una aplicación renderizada en el servidor
  (usando plantillas de Django), que parece ser el enfoque actual del proyecto.
- Este archivo (`evaluaciones/views.py`) contenía endpoints de API que no se estaban
  utilizando.

Para evitar duplicación y confusión, el contenido ha sido eliminado. Toda la
funcionalidad del test y del dashboard es manejada desde `frontend/views.py`.
"""
